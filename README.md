# ETC Validation

## Build

Build dynamorio (from root directory) 
```
cd ext/dynamorio
mkdir build
cd build
cmake ..
make -j
make install
```

Build project (from root directory)
```
mkdir build
cd build
cmake ..
cmake --build .
```
